---
📅 **날짜**: 2024년 11월 20일 (수)
👤 **작성자**: 강철원 (연구책임자) | **승인**: 강혜림 (대표)
📊 **진행 단계**: 1단계 - 기초연구 및 설계
🎯 **주요 작업**: VALL-E X 모듈화
---

# AI 기반 다국어 음성 합성 및 실시간 립싱크 더빙 시스템 개발일지

## 📋 오늘의 작업 내용

### 1. 모듈 래핑

- 복잡한 VALL-E X 코드를 `TTSGenerator` 클래스로 래핑.
- `generate(text, prompt_path, language)` 메서드 제공.
- 모델 로딩 로직을 클래스 초기화 시점으로 이동.

### 2. 통합 테스트 (Text -> TTS)

- 번역된 텍스트 리스트를 순회하며 오디오 파일 생성.
- **결과**: `output_001.wav`, `output_002.wav` ... 순차 생성 확인.

## 🔧 기술적 진행사항

### Class Design

```python
class TTSGenerator:
    def __init__(self):
        self.model = load_vallex_model()
    
    def generate(self, text, prompt):
        # ... inference logic ...
        return audio
```

## 📊 진행 상황

| 항목 | 계획 | 실제 | 상태 |
|------|------|------|------|
| 모듈화 | 완료 | 완료 | ✅ |
| 통합 테스트 | 완료 | 완료 | ✅ |

## 🚧 이슈 사항 및 해결 방안

- **긴 문장**: 문장이 너무 길면(20초 이상) 생성이 끊기거나 품질 저하. -> 문장 분리(Sentence Splitting) 전처리 필요.

## 📝 내일 계획

1. 문장 분리 로직 구현
2. 11월 3주차 주간 정리

---

## 📚 참고 자료

- [1] "TTS Long Text Handling".

<details>
<summary>IRIS 붙여넣기용 HTML 코드</summary>

```html
<h3>1. 모듈 래핑</h3>
<ul>
<li>복잡한 VALL-E X 코드를 <code>TTSGenerator</code> 클래스로 래핑.</li>
<li><code>generate(text, prompt_path, language)</code> 메서드 제공.</li>
<li>모델 로딩 로직을 클래스 초기화 시점으로 이동.</li>
</ul>
<h3>2. 통합 테스트 (Text -&gt; TTS)</h3>
<ul>
<li>번역된 텍스트 리스트를 순회하며 오디오 파일 생성.</li>
<li><strong>결과</strong>: <code>output_001.wav</code>, <code>output_002.wav</code> ... 순차 생성 확인.</li>
</ul>
```

</details>
