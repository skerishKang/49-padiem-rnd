---
📅 **날짜**: 2024년 12월 4일 (수)
👤 **작성자**: 강철원 (연구책임자) | **승인**: 강혜림 (대표)
📊 **진행 단계**: 2단계 - 핵심기술개발
🎯 **주요 작업**: 특수문자 처리 및 전처리 통합
---

# AI 기반 다국어 음성 합성 및 실시간 립싱크 더빙 시스템 개발일지

## 📋 오늘의 작업 내용

### 1. 특수문자 제거

- TTS 발화에 불필요한 괄호 `()`, `[]` 및 내용 제거.
- 예: "안녕하세요 (웃음)" -> "안녕하세요".
- 정규식 `re.sub(r'\([^)]*\)', '', text)` 활용.

### 2. 전처리 모듈 통합

- `modules/text/preprocessor.py` 생성.
- `TextPreprocessor` 클래스에 정규화, 정제, 분리 로직 캡슐화.

## 🔧 기술적 진행사항

### Class Structure

```python
class TextPreprocessor:
    def process(self, text, lang):
        text = self.clean(text)
        text = self.normalize(text, lang)
        sentences = self.split(text, lang)
        return sentences
```

## 📊 진행 상황

| 항목 | 계획 | 실제 | 상태 |
|------|------|------|------|
| 특수문자 처리 | 완료 | 완료 | ✅ |
| 모듈 통합 | 완료 | 완료 | ✅ |

## 🚧 이슈 사항 및 해결 방안

- **특이사항 없음**.

## 📝 내일 계획

1. 전처리 모듈 단위 테스트
2. 번역 모듈과 연동

---

## 📚 참고 자료

- [1] "Regex for Text Cleaning".

<details>
<summary>IRIS 붙여넣기용 HTML 코드</summary>

```html
<h3>1. 특수문자 제거</h3>
<ul>
<li>TTS 발화에 불필요한 괄호 <code>()</code>, <code>[]</code> 및 내용 제거.</li>
<li>예: "안녕하세요 (웃음)" -&gt; "안녕하세요".</li>
<li>정규식 <code>re.sub(r'\([^)]*\)', '', text)</code> 활용.</li>
</ul>
<h3>2. 전처리 모듈 통합</h3>
<ul>
<li><code>modules/text/preprocessor.py</code> 생성.</li>
<li><code>TextPreprocessor</code> 클래스에 정규화, 정제, 분리 로직 캡슐화.</li>
</ul>
```

</details>
