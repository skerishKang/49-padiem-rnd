---
📅 **날짜**: 2025년 1월 8일 (수)
👤 **작성자**: 강철원 (연구책임자) | **승인**: 강혜림 (대표)
📊 **진행 단계**: 2단계 - 핵심기술개발
🎯 **주요 작업**: 피치 조절 연구
---

# AI 기반 다국어 음성 합성 및 실시간 립싱크 더빙 시스템 개발일지

## 📋 오늘의 작업 내용

### 1. 피치 불일치 문제

- VALL-E X로 생성된 TTS 음성의 피치와 타겟 화자(RVC 모델)의 피치 대역이 다르면 변환 품질 저하.
- 예: TTS는 높은 톤인데 RVC 모델은 낮은 톤인 경우 찢어지는 소리 발생.

### 2. 자동 피치 매칭

- `librosa`로 소스 오디오와 타겟 오디오의 평균 피치(f0)를 계산.
- 차이만큼 `f0_up_key`를 자동으로 설정하는 로직 구현.
- **결과**: 수동 조절 없이도 안정적인 변환 가능.

## 🔧 기술적 진행사항

### Pitch Calculation

```python
f0_source = librosa.yin(source_audio, ...)
f0_target = librosa.yin(target_audio, ...)
semitone_diff = 12 * np.log2(f0_target.mean() / f0_source.mean())
```

## 📊 진행 상황

| 항목 | 계획 | 실제 | 상태 |
|------|------|------|------|
| 피치 분석 | 완료 | 완료 | ✅ |
| 자동화 로직 | 완료 | 완료 | ✅ |

## 🚧 이슈 사항 및 해결 방안

- **성별 차이**: 성별이 다를 경우 피치 차이가 커서 자동 로직만으로는 한계. -> 성별 감지(Gender Detection) 후 기본 오프셋(+12/-12) 적용 로직 추가.

## 📝 내일 계획

1. RVC 모듈 패키징
2. 주간 정리

---

## 📚 참고 자료

- [1] "Librosa Pitch Tracking".

<details>
<summary>IRIS 붙여넣기용 HTML 코드</summary>

```html
<h3>1. 피치 불일치 문제</h3>
<ul>
<li>VALL-E X로 생성된 TTS 음성의 피치와 타겟 화자(RVC 모델)의 피치 대역이 다르면 변환 품질 저하.</li>
<li>예: TTS는 높은 톤인데 RVC 모델은 낮은 톤인 경우 찢어지는 소리 발생.</li>
</ul>
<h3>2. 자동 피치 매칭</h3>
<ul>
<li><code>librosa</code>로 소스 오디오와 타겟 오디오의 평균 피치(f0)를 계산.</li>
<li>차이만큼 <code>f0_up_key</code>를 자동으로 설정하는 로직 구현.</li>
<li><strong>결과</strong>: 수동 조절 없이도 안정적인 변환 가능.</li>
</ul>
```

</details>
