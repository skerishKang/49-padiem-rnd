---
📅 **날짜**: 2025년 1월 13일 (월)
👤 **작성자**: 강철원 (연구책임자) | **승인**: 강혜림 (대표)
📊 **진행 단계**: 2단계 - 핵심기술개발
🎯 **주요 작업**: 하이브리드 파이프라인 통합
---

# AI 기반 다국어 음성 합성 및 실시간 립싱크 더빙 시스템 개발일지

## 📋 오늘의 작업 내용

### 1. 파이프라인 설계

- `TTS` -> `RVC` 연결.
- VALL-E X는 "발음(Pronunciation)"과 "언어(Language)"를 담당.
- RVC는 "음색(Timbre)"을 담당.
- VALL-E X 생성 시 타겟 화자의 목소리를 프롬프트로 쓰되, RVC로 한 번 더 입혀서 유사도 극대화.

### 2. 구현

- `orchestrator/pipeline.py` 수정.
- TTS 생성 결과를 임시 파일로 저장 후 RVC 입력으로 전달.

## 🔧 기술적 진행사항

### Flow

```python
tts_audio = tts.generate(text, prompt)
rvc_audio = rvc.convert(tts_audio, model_path)
```

## 📊 진행 상황

| 항목 | 계획 | 실제 | 상태 |
|------|------|------|------|
| 설계 | 완료 | 완료 | ✅ |
| 구현 | 완료 | 완료 | ✅ |

## 🚧 이슈 사항 및 해결 방안

- **레이턴시**: 두 모델을 연속으로 돌리니 시간이 2배 소요. -> 병렬 처리는 불가능(의존성). 최적화 필요.

## 📝 내일 계획

1. 통합 테스트
2. 품질 비교

---

## 📚 참고 자료

- [1] "Cascaded Models".

<details>
<summary>IRIS 붙여넣기용 HTML 코드</summary>

```html
<h3>1. 파이프라인 설계</h3>
<ul>
<li><code>TTS</code> -&gt; <code>RVC</code> 연결.</li>
<li>VALL-E X는 "발음(Pronunciation)"과 "언어(Language)"를 담당.</li>
<li>RVC는 "음색(Timbre)"을 담당.</li>
<li>VALL-E X 생성 시 타겟 화자의 목소리를 프롬프트로 쓰되, RVC로 한 번 더 입혀서 유사도 극대화.</li>
</ul>
<h3>2. 구현</h3>
<ul>
<li><code>orchestrator/pipeline.py</code> 수정.</li>
<li>TTS 생성 결과를 임시 파일로 저장 후 RVC 입력으로 전달.</li>
</ul>
```

</details>
