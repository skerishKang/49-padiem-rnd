---
📅 **날짜**: 2025년 2월 4일 (화)
👤 **작성자**: 강철원 (연구책임자) | **승인**: 강혜림 (대표)
📊 **진행 단계**: 2단계 - 핵심기술개발
🎯 **주요 작업**: Wav2Lip 환경 구축
---

# AI 기반 다국어 음성 합성 및 실시간 립싱크 더빙 시스템 개발일지

## 📋 오늘의 작업 내용

### 1. 의존성 설치

- `librosa`, `opencv-python`, `tqdm` 등 설치.
- 기존 RVC/TTS 환경과 충돌 없는지 확인. (PyTorch 버전 호환됨).

### 2. 모델 다운로드

- `wav2lip.pth` (일반 모델) 및 `wav2lip_gan.pth` (GAN 적용 모델) 다운로드.
- `s3fd` (얼굴 감지 모델) 다운로드.

## 🔧 기술적 진행사항

### Setup

- `checkpoints/` 폴더에 모델 파일 배치.

## 📊 진행 상황

| 항목 | 계획 | 실제 | 상태 |
|------|------|------|------|
| 설치 | 완료 | 완료 | ✅ |
| 모델 준비 | 완료 | 완료 | ✅ |

## 🚧 이슈 사항 및 해결 방안

- **얼굴 감지**: `s3fd` 모델이 오래되어 최신 PyTorch에서 경고 발생. -> 무시해도 되거나 코드 일부 수정.

## 📝 내일 계획

1. 추론 테스트
2. 얼굴 감지 성능 확인

---

## 📚 참고 자료

- [1] "Wav2Lip Installation".

<details>
<summary>IRIS 붙여넣기용 HTML 코드</summary>

```html
<h3>1. 의존성 설치</h3>
<ul>
<li><code>librosa</code>, <code>opencv-python</code>, <code>tqdm</code> 등 설치.</li>
<li>기존 RVC/TTS 환경과 충돌 없는지 확인. (PyTorch 버전 호환됨).</li>
</ul>
<h3>2. 모델 다운로드</h3>
<ul>
<li><code>wav2lip.pth</code> (일반 모델) 및 <code>wav2lip_gan.pth</code> (GAN 적용 모델) 다운로드.</li>
<li><code>s3fd</code> (얼굴 감지 모델) 다운로드.</li>
</ul>
```

</details>
