---
📅 **날짜**: 2025년 3월 6일 (목)
👤 **작성자**: 강철원 (연구책임자) | **승인**: 강혜림 (대표)
📊 **진행 단계**: 3단계 - 시스템 통합
🎯 **주요 작업**: Celery 연동
---

# AI 기반 다국어 음성 합성 및 실시간 립싱크 더빙 시스템 개발일지

## 📋 오늘의 작업 내용

### 1. Celery 설정

- `app/core/celery_app.py` 생성.
- Redis 브로커 URL 설정 (`redis://localhost:6379/0`).

### 2. 태스크 정의

- `app/worker.py`에 `process_video_task` 정의.
- 기존 `pipeline_runner.py`의 로직을 이 태스크 안으로 이동.

## 🔧 기술적 진행사항

### Task

```python
@celery_app.task(bind=True)
def process_video_task(self, file_path, lang):
    # Run AI pipeline
    return result_path
```

## 📊 진행 상황

| 항목 | 계획 | 실제 | 상태 |
|------|------|------|------|
| Celery 설정 | 완료 | 완료 | ✅ |
| 태스크 구현 | 완료 | 완료 | ✅ |

## 🚧 이슈 사항 및 해결 방안

- **윈도우 Celery**: 윈도우에서 Celery 4.x 이상 실행 시 `pool=solo` 옵션을 줘야 함 (Fork 미지원 때문).
- `celery -A app.core.celery_app worker --pool=solo -l info`.

## 📝 내일 계획

1. 주간 정리
2. Redis 구축 및 연동 테스트

---

## 📚 참고 자료

- [1] "Celery on Windows".

<details>
<summary>IRIS 붙여넣기용 HTML 코드</summary>

```html
<h3>1. Celery 설정</h3>
<ul>
<li><code>app/core/celery_app.py</code> 생성.</li>
<li>Redis 브로커 URL 설정 (<code>redis://localhost:6379/0</code>).</li>
</ul>
<h3>2. 태스크 정의</h3>
<ul>
<li><code>app/worker.py</code>에 <code>process_video_task</code> 정의.</li>
<li>기존 <code>pipeline_runner.py</code>의 로직을 이 태스크 안으로 이동.</li>
</ul>
```

</details>
