---
📅 **날짜**: 2025년 4월 8일 (화)
👤 **작성자**: 강철원 (연구책임자) | **승인**: 강혜림 (대표)
📊 **진행 단계**: 4단계 - 시스템 고도화
🎯 **주요 작업**: Redis 캐싱 구현
---

# AI 기반 다국어 음성 합성 및 실시간 립싱크 더빙 시스템 개발일지

## 📋 오늘의 작업 내용

### 1. 구현

- `modules/utils/cache.py` 작성.
- 데코레이터 패턴(`@cached`)을 사용하여 함수 호출 결과를 Redis에 저장/조회.
- 오디오 데이터는 Redis에 직접 저장하기엔 크므로, 파일 시스템에 저장하고 경로만 Redis에 저장.

### 2. 적용

- `DeepTranslator.translate`, `VALLEX.generate`, `RVC.convert` 메서드에 적용.

## 🔧 기술적 진행사항

### Decorator

```python
def cached(key_prefix):
    def decorator(func):
        # check redis
        # if hit, return
        # else, run func, save to redis
```

## 📊 진행 상황

| 항목 | 계획 | 실제 | 상태 |
|------|------|------|------|
| 모듈 구현 | 완료 | 완료 | ✅ |
| 적용 | 완료 | 완료 | ✅ |

## 🚧 이슈 사항 및 해결 방안

- **TTL**: 캐시 만료 시간 설정 필요. 오디오 파일은 용량을 차지하므로 LRU(Least Recently Used) 정책으로 오래된 파일 삭제 필요. -> 일단 24시간 TTL 설정.

## 📝 내일 계획

1. 모델 서빙 최적화 (TorchScript)
2. 성능 비교

---

## 📚 참고 자료

- [1] "Redis Cache Pattern".

<details>
<summary>IRIS 붙여넣기용 HTML 코드</summary>

```html
<h3>1. 구현</h3>
<ul>
<li><code>modules/utils/cache.py</code> 작성.</li>
<li>데코레이터 패턴(<code>@cached</code>)을 사용하여 함수 호출 결과를 Redis에 저장/조회.</li>
<li>오디오 데이터는 Redis에 직접 저장하기엔 크므로, 파일 시스템에 저장하고 경로만 Redis에 저장.</li>
</ul>
<h3>2. 적용</h3>
<ul>
<li><code>DeepTranslator.translate</code>, <code>VALLEX.generate</code>, <code>RVC.convert</code> 메서드에 적용.</li>
</ul>
```

</details>
