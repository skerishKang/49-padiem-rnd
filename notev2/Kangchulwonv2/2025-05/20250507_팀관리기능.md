---
📅 **날짜**: 2025년 5월 7일 (수)
👤 **작성자**: 강철원 (연구책임자) | **승인**: 강혜림 (대표)
📊 **진행 단계**: 3단계 - 고도화 및 사업화
🎯 **주요 작업**: 엔터프라이즈 팀 관리 기능 개발
---

# AI 기반 다국어 음성 합성 및 실시간 립싱크 더빙 시스템 개발일지

## 📋 오늘의 작업 내용

### 1. 팀(Team) 모델 구현

- **구조**: 하나의 팀에 여러 사용자가 소속될 수 있는 N:M 관계가 아닌, 1:N 관계(한 사용자는 하나의 메인 팀에 소속)로 단순화 후 확장 고려. 일단은 N:M 지원을 위해 `team_members` 연결 테이블 생성.
- **권한**: Owner(소유자), Admin(관리자), Member(일반), Viewer(읽기 전용).

### 2. 초대 시스템 개발

- **방식**: 이메일로 초대 링크 발송 -> 클릭 시 팀 합류.
- **토큰**: JWT를 사용하여 초대 링크의 유효성(만료 시간 24시간) 검증.

## 🔧 기술적 진행사항

### 초대 링크 생성

```python
def create_invitation_link(team_id, email):
    token = create_access_token(data={"sub": email, "team_id": team_id}, expires_delta=timedelta(hours=24))
    return f"{BASE_URL}/invite?token={token}"
```

## 📊 진행 상황

| 항목 | 계획 | 실제 | 상태 |
|------|------|------|------|
| DB 모델링 | 완료 | 완료 | ✅ |
| 초대 로직 구현 | 완료 | 완료 | ✅ |

## 🚧 이슈 사항 및 해결 방안

- **동시성 문제**: 동시에 여러 팀에 초대받을 경우 처리. -> 트랜잭션 격리 수준을 `READ COMMITTED`로 설정하고, 애플리케이션 레벨에서 중복 가입 방지 로직 추가.

## 📝 내일 계획

1. 팀 대시보드 UI 개발 (Streamlit)
2. 권한별 접근 제어(RBAC) 테스트

---

## 📚 참고 자료

- [1] "RBAC Implementation". [Link](https://en.wikipedia.org/wiki/Role-based_access_control)

<details>
<summary>IRIS 붙여넣기용 HTML 코드</summary>

```html
<h3>1. 팀(Team) 모델 구현</h3>
<ul>
<li><strong>구조</strong>: 하나의 팀에 여러 사용자가 소속될 수 있는 N:M 관계가 아닌, 1:N 관계(한 사용자는 하나의 메인 팀에 소속)로 단순화 후 확장 고려. 일단은 N:M 지원을 위해 <code>team_members</code> 연결 테이블 생성.</li>
<li><strong>권한</strong>: Owner(소유자), Admin(관리자), Member(일반), Viewer(읽기 전용).</li>
</ul>
<h3>2. 초대 시스템 개발</h3>
<ul>
<li><strong>방식</strong>: 이메일로 초대 링크 발송 -&gt; 클릭 시 팀 합류.</li>
<li><strong>토큰</strong>: JWT를 사용하여 초대 링크의 유효성(만료 시간 24시간) 검증.</li>
</ul>
```

</details>
