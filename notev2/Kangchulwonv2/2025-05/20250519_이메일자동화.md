---
📅 **날짜**: 2025년 5월 19일 (월)
👤 **작성자**: 강철원 (연구책임자) | **승인**: 강혜림 (대표)
📊 **진행 단계**: 3단계 - 고도화 및 사업화
🎯 **주요 작업**: 이메일 발송 비동기 처리
---

# AI 기반 다국어 음성 합성 및 실시간 립싱크 더빙 시스템 개발일지

## 📋 오늘의 작업 내용

### 1. Celery Task 구현

- 이메일 발송은 I/O 바운드 작업이므로 메인 스레드를 차단하지 않도록 Celery 워커로 위임.
- **재시도 로직**: 네트워크 오류 등으로 실패 시 지수 백오프(Exponential Backoff)로 재시도.

### 2. 트리거 설정

- **회원가입**: `send_welcome_email.delay(user_id)`
- **작업 완료**: `send_job_complete_email.delay(user_id, job_id)`
- **비밀번호 찾기**: `send_password_reset_email.delay(email, token)`

## 🔧 기술적 진행사항

### Celery Task

```python
@celery_app.task(bind=True, max_retries=3)
def send_email_task(self, to_email, subject, body):
    try:
        ses.send_email(...)
    except Exception as e:
        self.retry(exc=e, countdown=60)
```

## 📊 진행 상황

| 항목 | 계획 | 실제 | 상태 |
|------|------|------|------|
| Task 구현 | 완료 | 완료 | ✅ |
| 트리거 연결 | 완료 | 완료 | ✅ |

## 🚧 이슈 사항 및 해결 방안

- **템플릿 관리**: 하드코딩된 HTML은 수정이 어려움. -> 별도의 `templates/email` 디렉토리에 HTML 파일로 분리하고 Jinja2로 렌더링.

## 📝 내일 계획

1. 이메일 템플릿 디자인 (HTML/CSS)
2. 수신 거부(Unsubscribe) 기능 구현

---

## 📚 참고 자료

- [1] "Celery Retries". [Link](https://docs.celeryq.dev/en/stable/userguide/tasks.html#retrying)

<details>
<summary>IRIS 붙여넣기용 HTML 코드</summary>

```html
<h3>1. Celery Task 구현</h3>
<ul>
<li>이메일 발송은 I/O 바운드 작업이므로 메인 스레드를 차단하지 않도록 Celery 워커로 위임.</li>
<li><strong>재시도 로직</strong>: 네트워크 오류 등으로 실패 시 지수 백오프(Exponential Backoff)로 재시도.</li>
</ul>
<h3>2. 트리거 설정</h3>
<ul>
<li><strong>회원가입</strong>: <code>send_welcome_email.delay(user_id)</code></li>
<li><strong>작업 완료</strong>: <code>send_job_complete_email.delay(user_id, job_id)</code></li>
<li><strong>비밀번호 찾기</strong>: <code>send_password_reset_email.delay(email, token)</code></li>
</ul>
```

</details>
