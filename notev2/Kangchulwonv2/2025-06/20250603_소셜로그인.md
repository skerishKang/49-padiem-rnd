---
📅 **날짜**: 2025년 6월 3일 (화)
👤 **작성자**: 강철원 (연구책임자) | **승인**: 강혜림 (대표)
📊 **진행 단계**: 3단계 - 시스템 통합 및 고도화
🎯 **주요 작업**: Backend 기본 구조 구현
---

# AI 기반 다국어 음성 합성 및 실시간 립싱크 더빙 시스템 개발일지

## 📋 오늘의 작업 내용

### 1. FastAPI 서버 구축

- `backend/main.py`: 앱 진입점 및 미들웨어(CORS) 설정.
- `backend/routers/`: 모듈별 라우터 분리 (`audio.py`, `stt.py`, `tts.py` 등).
- **CORS 설정**: Streamlit 프론트엔드(`localhost:8501`)에서의 접근 허용.

### 2. Job Manager 구현

- 비동기 작업 관리를 위한 `JobManager` 클래스 설계.
- 작업 상태(Pending, Processing, Completed, Failed) 추적 기능.
- 메모리 내 딕셔너리를 사용한 간단한 상태 저장소 구현.

## 🔧 기술적 진행사항

### Router Include

```python
app.include_router(audio.router)
app.include_router(stt.router)
# ... 기타 라우터 추가
```

## 📊 진행 상황

| 항목 | 계획 | 실제 | 상태 |
|------|------|------|------|
| 서버 기본 설정 | 완료 | 완료 | ✅ |
| 라우터 분리 | 완료 | 완료 | ✅ |

## 🚧 이슈 사항 및 해결 방안

- **의존성 충돌**: `torch` 버전 호환성 문제. -> 가상환경 분리(`venv_backend`) 및 `requirements.txt` 명시로 해결.

## 📝 내일 계획

1. 오디오 추출 모듈 API 연동
2. 파일 업로드 처리

---

## 📚 참고 자료

- [1] "FastAPI Router". [Link](https://fastapi.tiangolo.com/tutorial/bigger-applications/#apirouter)

<details>
<summary>IRIS 붙여넣기용 HTML 코드</summary>

```html
<h3>1. FastAPI 서버 구축</h3>
<ul>
<li><code>backend/main.py</code>: 앱 진입점 및 미들웨어(CORS) 설정.</li>
<li><code>backend/routers/</code>: 모듈별 라우터 분리 (<code>audio.py</code>, <code>stt.py</code>, <code>tts.py</code> 등).</li>
<li><strong>CORS 설정</strong>: Streamlit 프론트엔드(<code>localhost:8501</code>)에서의 접근 허용.</li>
</ul>
<h3>2. Job Manager 구현</h3>
<ul>
<li>비동기 작업 관리를 위한 <code>JobManager</code> 클래스 설계.</li>
<li>작업 상태(Pending, Processing, Completed, Failed) 추적 기능.</li>
<li>메모리 내 딕셔너리를 사용한 간단한 상태 저장소 구현.</li>
</ul>
```

</details>
