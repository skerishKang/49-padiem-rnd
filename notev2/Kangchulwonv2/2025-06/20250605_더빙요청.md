---
📅 **날짜**: 2025년 6월 5일 (목)
👤 **작성자**: 강철원 (연구책임자) | **승인**: 강혜림 (대표)
📊 **진행 단계**: 3단계 - 시스템 통합 및 고도화
🎯 **주요 작업**: STT 모듈 연동 및 UI 구현
---

# AI 기반 다국어 음성 합성 및 실시간 립싱크 더빙 시스템 개발일지

## 📋 오늘의 작업 내용

### 1. STT API 구현

- `backend/routers/stt.py`: OpenAI Whisper 모델 로드 및 추론.
- **옵션**: 모델 크기(base, small, medium) 선택 가능하도록 파라미터화.
- **결과**: 텍스트 및 타임스탬프(SRT 형식) 반환.

### 2. Streamlit UI (1단계)

- `frontend/steps/audio.py`: 파일 업로더 위젯 구현.
- `frontend/steps/stt.py`: STT 실행 버튼 및 결과 텍스트 에디터 구현.

## 🔧 기술적 진행사항

### Whisper Integration

```python
model = whisper.load_model("base")
result = model.transcribe(audio_path)
```

## 📊 진행 상황

| 항목 | 계획 | 실제 | 상태 |
|------|------|------|------|
| STT API | 완료 | 완료 | ✅ |
| UI 구현 | 완료 | 완료 | ✅ |

## 🚧 이슈 사항 및 해결 방안

- **GPU 메모리**: Whisper 모델 로드 시 VRAM 점유율 상승. -> 사용 후 `del model`, `torch.cuda.empty_cache()` 호출하여 메모리 해제.

## 📝 내일 계획

1. 번역 모듈 (Deep Translator) 연동
2. TTS (VALL-E X) 연동 준비

---

## 📚 참고 자료

- [1] "OpenAI Whisper". [Link](https://github.com/openai/whisper)

<details>
<summary>IRIS 붙여넣기용 HTML 코드</summary>

```html
<h3>1. STT API 구현</h3>
<ul>
<li><code>backend/routers/stt.py</code>: OpenAI Whisper 모델 로드 및 추론.</li>
<li><strong>옵션</strong>: 모델 크기(base, small, medium) 선택 가능하도록 파라미터화.</li>
<li><strong>결과</strong>: 텍스트 및 타임스탬프(SRT 형식) 반환.</li>
</ul>
<h3>2. Streamlit UI (1단계)</h3>
<ul>
<li><code>frontend/steps/audio.py</code>: 파일 업로더 위젯 구현.</li>
<li><code>frontend/steps/stt.py</code>: STT 실행 버튼 및 결과 텍스트 에디터 구현.</li>
</ul>
```

</details>
