---
📅 **날짜**: 2025년 7월 8일 (화)
👤 **작성자**: 강철원 (연구책임자) | **승인**: 강혜림 (대표)
📊 **진행 단계**: 3단계 - 고도화 및 사업화
🎯 **주요 작업**: 구간 선택 및 서버 연동
---

# AI 기반 다국어 음성 합성 및 실시간 립싱크 더빙 시스템 개발일지

## 📋 오늘의 작업 내용

### 1. 인터랙션 구현

- 사용자가 파형 위를 드래그하면 Region 생성.
- 생성된 Region의 시작 시간과 종료 시간을 실시간으로 Input 필드에 표시.
- '구간 추가' 버튼으로 여러 구간 설정 가능.

### 2. 서버 전송

- 더빙 요청 시 `segments` 리스트(Start, End, Text(Optional))를 함께 전송.
- 백엔드에서는 전체 영상이 아닌 해당 세그먼트만 처리하도록 로직 분기.

## 🔧 기술적 진행사항

### JavaScript (Client)

```javascript
wavesurfer.addRegion({
    start: 10,
    end: 20,
    color: 'rgba(0, 255, 0, 0.1)'
});

wavesurfer.on('region-update-end', function(region) {
    Streamlit.setComponentValue({start: region.start, end: region.end});
});
```

## 📊 진행 상황

| 항목 | 계획 | 실제 | 상태 |
|------|------|------|------|
| 인터랙션 | 완료 | 완료 | ✅ |
| 데이터 전송 | 완료 | 완료 | ✅ |

## 🚧 이슈 사항 및 해결 방안

- **Streamlit 리렌더링**: 값 변경 시마다 전체 페이지가 리로드되는 문제. -> `st.session_state`와 콜백 함수를 적절히 사용하여 상태 유지.

## 📝 내일 계획

1. 백엔드 세그먼트 처리 로직 구현
2. 영상 병합 테스트

---

## 📚 참고 자료

- [1] "Streamlit Components". [Link](https://docs.streamlit.io/library/components)

<details>
<summary>IRIS 붙여넣기용 HTML 코드</summary>

```html
<h3>1. 인터랙션 구현</h3>
<ul>
<li>사용자가 파형 위를 드래그하면 Region 생성.</li>
<li>생성된 Region의 시작 시간과 종료 시간을 실시간으로 Input 필드에 표시.</li>
<li>'구간 추가' 버튼으로 여러 구간 설정 가능.</li>
</ul>
<h3>2. 서버 전송</h3>
<ul>
<li>더빙 요청 시 <code>segments</code> 리스트(Start, End, Text(Optional))를 함께 전송.</li>
<li>백엔드에서는 전체 영상이 아닌 해당 세그먼트만 처리하도록 로직 분기.</li>
</ul>
```

</details>
